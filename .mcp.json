{
  "mcpServers": {
    "code-rag-mcp": {
      "type": "stdio",
      "command": "${CLAUDE_PLUGIN_ROOT}/venv/bin/python",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/mcp_server.py"],
      "env": {
        "CODE_RAG_DATA": "${HOME}/.code-rag-mcp"
      }
    }
  }
}
